<UserControl x:Class="GreenhouseDesktopApp.Views.GreenhouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:GreenhouseDesktopApp.Views"
             xmlns:components="clr-namespace:GreenhouseDesktopApp.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <components:TabsView Grid.Row="0" />
        <views:AppSettingsView Grid.Row="1" />
    </Grid>
</UserControl>
