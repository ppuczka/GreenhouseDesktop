<Window x:Class="GreenhouseDesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GreenhouseDesktopApp"
        xmlns:views="clr-namespace:GreenhouseDesktopApp.Views"
        Title="Greenhouse Controller" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Greenhouse Controller" FontSize="25" FontWeight="Bold" Margin="10,0,10,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        <Button Grid.Column="1" Content="Connect" Padding="10,5" HorizontalAlignment="Center" Margin="0,5,5,15"/>

        <views:GreenhouseView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="2"/>

    </Grid>
</Window>
