<UserControl x:Class="GreenhouseDesktopApp.Components.AppSettingsDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenhouseDesktopApp.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <StackPanel>
            <TextBlock Text="App Setting Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Select a setting from the list to view or edit its details." TextWrapping="Wrap"/>
            <!-- Add more detailed settings UI elements here -->
        </StackPanel>

    </Grid>
</UserControl>
